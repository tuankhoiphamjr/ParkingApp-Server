Auth: POST
    api/auth/signin: {phoneNumber, password}
    api/auth/signup: {phoneNumber, password, firstName, lastName, email}
    api/auth/signout {id: user._id}

Avatar: GET
    api/avatar/file/:id - get JSON of image with {:id}
    api/avatar/:id - get image with {:id}